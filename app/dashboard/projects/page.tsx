// my projects (create/search user projects)
// want to show user projects and have create btn
// will then have sub pages for specific project [project_id]/edit or [project_id]/create?
// then maybe cascade this for items within a project once on a specific project page (then do similar to invoices with edit/create item?)


export default function Page() {
  return (
    <div>
      <div>Hello from user projects</div>
    </div>
  );
}