export type UserProjects = {
    id: string;
    name: string;
    description: string;
    created_at: string;
}

export type UserProjectsTable = {
    id: string;
    name: string;
    description: string;
    created_at: string;
}